object Buggy {
  //Exercise #4: Input with prompts
  def main(args: Array[String]): Unit = {
    var single: Char = 'a' // cannot be any system name such as def. char typed elements should be in single  quotes
    var FIVE: Int = 5 //integer data type can store only whole number, 5.1 is not acceptable

    println("The data is "+ single + " and "+FIVE+"!") // the println method should be closed by )
  }
}